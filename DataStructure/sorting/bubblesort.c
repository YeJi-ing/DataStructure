// input: 1. 배열의 맨 앞 주소 
//		  2. 배열의 크기
// output: 배열을 오름차순으로 정렬


/*
void bubblesort(int* arr, int size)
{
	int i, j;
	for (i = 0; i < size; i++)
	{
		for (j = 0; j < size - 1 - i; j++)
		{
			if (arr[j] > arr[j + 1])
			{
				swap(arr, j, j + 1);
			}
		}
	}
}
*/